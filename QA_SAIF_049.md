# SAIF-049 — حد الاستخدام في `/api/chat` غير موزع ولا دائم

- **الخطورة:** متوسطة/عالية — إساءة استخدام / تكلفة / موثوقية API.
- **الموضع:** `api/chat.ts` — `rateLimitStore = new Map<string, RateEntry>()` و `checkRateLimit()`.
- **الوصف:** حد 30 طلباً/10 دقائق محفوظ داخل ذاكرة عملية الـFunction فقط. في بيئة Vercel serverless لا توجد ضمانة أن الطلبات المتتالية ستصل إلى نفس النسخة، كما أن الـcold start أو إنشاء instance جديدة يعيد الخريطة فارغة. لذلك الحد المعلن ليس حدّاً خادمياً موزعاً يمكن الاعتماد عليه لمنع الاستهلاك أو التحكم بالتكلفة.
- **طريقة إعادة المشكلة:** استخدم جلسة صحيحة وأرسل طلبات متوازية/متتابعة إلى `/api/chat` بحيث تُخدم عبر أكثر من instance أو بعد إعادة تهيئة Function؛ راقب أن العداد لا يُشارك بين النسخ ويمكن أن يتجاوز مجموع الطلبات 30 خلال النافذة دون 429 موثوق.
- **الأثر:** إمكانية تجاوز rate limit واستهلاك مفاتيح Gemini/الحصة والتكلفة، مع اختلاف السلوك حسب instance.
- **الإصلاح المطلوب:** نقل العداد إلى مخزن موزع ذي TTL (مثل Redis/KV/Upstash أو خدمة rate-limit مناسبة)، بمفتاح يربط الجلسة/المستخدم وربما IP وفق سياسة الخصوصية، وتنفيذ increment ذري مع نافذة زمنية موحدة. لا تعتمد على `Map` داخل Function كحاجز أمني أو مالي.
- **الحالة:** مفتوح — مؤكد من الكود. لم أعتبر نجاح البناء دليلاً على نجاح هذا الضابط.

> ملاحظة دمج: يجب نقل هذا السجل إلى `QA_SAIF_REPORT.md` في أول تحديث كامل آمن للتقرير، دون تكراره، لأن أداة قراءة التقرير أعادت المحتوى الكبير مبتوراً في هذه الجولة ولا يصح استبدال الملف الكامل بنسخة ناقصة.
