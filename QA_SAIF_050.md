# SAIF-050 — مسار تحويل القصة كان يرسل المعرّفات الحساسة إلى مزود الذكاء الاصطناعي

- **الخطورة:** P0 — خصوصية.
- **الموضع:** `api/convert-story.ts`.
- **الاكتشاف:** 2026-09-21.
- **الوصف:** كان المسار يأخذ `story` من المستخدم ويضعه حرفياً داخل prompt ثم يرسله إلى Vercel AI Gateway أو Gemini. لذلك إذا احتوى السرد على رقم هوية/إقامة أو بيانات اتصال أو IBAN فإنها كانت تصل إلى مزود الذكاء الاصطناعي.
- **طريقة الإعادة قبل الإصلاح:** إرسال `POST /api/convert-story` بجلسة صالحة ونص قصة يتضمن رقماً وطنياً من 10 أرقام يبدأ بـ1 أو2؛ كان الطلب يواصل التنفيذ إلى `askAi(prompt)` دون بوابة خصوصية.
- **الإصلاح:** أضيفت بوابة خادمية قبل أي استدعاء لمزود الذكاء الاصطناعي. ترفض القصة بـHTTP 422 و`SENSITIVE_DATA_DETECTED` عند اكتشاف رقم هوية/إقامة سعودي بالشكل المعتاد، IBAN سعودي، رقم جوال سعودي أو بريد إلكتروني. لا يُنشأ fallback قانوني عند الرفض.
- **Commit الإصلاح:** `e3661eac49e73388635b57d4c225b02ec1035939`.
- **التحقق الساكن:** بوابة الخصوصية تقع قبل `askAi(prompt)`، والمسار ما زال يفرض جلسة خادمية ويعيد 503 عند فشل المزود و502 عند استجابة AI غير الصالحة بدلاً من إنشاء نتيجة قانونية بديلة.
- **حالة البناء/النشر:** لم يعتمد بعد. حالة Vercel للـcommit كانت Pending عند الفحص، بينما اتصال Vercel المتاح أعاد 0 Teams؛ لا يُعتبر الإصلاح منشوراً حتى نجاح البناء/Preview والتحقق.
- **الحالة:** **تم الإصلاح في master، بانتظار تحقق البناء وPreview/Production.**

> ملاحظة تقريرية: يجب دمج هذا البند في `QA_SAIF_REPORT.md` عند أول تحديث تجميعي للتقرير. لم يُعاد كتابة تاريخ Git، وSAIF-009 بقي كما هو.
