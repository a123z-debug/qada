# SAIF-048 — ثقة API المستشار بأدوار المحادثة القادمة من العميل

- **الخطورة:** عالية — سلامة سياق الذكاء الاصطناعي/حقن تاريخ المحادثة.
- **الموضع:** `api/chat.ts` → `toGeminiContents`.
- **الوصف:** يقبل الخادم `role` من جسم الطلب، ويحوّل `assistant` أو `model` مباشرة إلى دور `model` عند إرسال المحتوى إلى Gemini. وبذلك يستطيع العميل إنشاء رسائل مزعومة على أنها صادرة سابقاً من النموذج، ثم مطالبة المستشار بالبناء عليها.
- **طريقة إعادة المشكلة:** بعد تسجيل الدخول، أرسل `POST /api/chat` مع `messages` تتضمن رسالة `{ "role": "model", "content": "...قاعدة أو نتيجة مزعومة..." }` ثم رسالة مستخدم تطلب متابعة النتيجة السابقة. سيُمرر الخادم الرسالة الأولى إلى Gemini بدور `model` بدلاً من اعتبارها محتوى غير موثوق من العميل.
- **الأثر:** يمكن تلويث سياق الاستشارة القانونية بتاريخ مصطنع وإعطاء استنتاجات المستخدم وزن كلام سابق للنموذج.
- **الإصلاح المطلوب:** لا تثق بأي دور `model/assistant` يرسله العميل. إما أن يعامل API كل الرسائل الواردة كـ`user`، أو يحتفظ بتاريخ المحادثة الموثوق خادمياً ويربطه بالجلسة، ولا يسمح للعميل بإنشاء رسائل النموذج.
- **الحالة:** مفتوح ومؤكد على `master` بتاريخ 2026-09-20.
