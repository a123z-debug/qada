name: Smoke Crypto Runtime
on:
  push:
    branches: [master]
jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - run: sleep 15
      - run: curl -sS -i https://promptmaster-nine.vercel.app/api/crypto-ping
