# تقرير سيف لفحص منصة أصول القضاء

آخر تحديث: 2026-09-19

> هذا سجل تراكمي للعيوب المؤكدة. لا يعني نجاح البناء أو النشر أن الوظائف اجتازت اختبار الجودة.

## SAIF-001 — حرج — بيانات دخول المدير مكشوفة في كود الواجهة

**المكان:** `src/components/LoginScreen.tsx`

**الوصف:** بيانات اعتماد حساب الإدارة، بما فيها رقم الهوية وكلمة المرور، معرفة كثوابت داخل حزمة React التي تصل إلى متصفح أي زائر. التحقق من دخول المدير يتم أيضاً داخل المتصفح فقط. أي شخص يستطيع قراءة JavaScript المنشور أو المستودع واستخراج بيانات المدير ثم إنشاء جلسة إدارية.

**طريقة إعادة المشكلة:**
1. افتح مصدر/حزمة الواجهة أو ملف `LoginScreen.tsx`.
2. ابحث عن `ADMIN_CREDENTIALS`.
3. ستظهر بيانات الدخول كاملة.
4. مسار `handleSubmit` يقارن المدخلات بهذه القيم محلياً ثم يمنح `role: 'admin'` بلا تحقق خادمي.

**الأثر:** تجاوز كامل للمصادقة الإدارية والوصول إلى الوظائف التي تعتمد على `session.role === 'admin'`. هذا يمنع اعتبار المنصة جاهزة للإنتاج.

**الإجراء المطلوب:** إزالة جميع أسرار الإدارة من الواجهة فوراً، تدوير كلمة المرور الحالية، ونقل المصادقة إلى خادم موثوق بجلسة موقعة/HttpOnly مع تحقق صلاحيات خادمي لكل عملية حساسة.

## SAIF-002 — عالٍ — كلمات مرور المستخدمين محفوظة كنص صريح في localStorage

**المكان:** `src/components/LoginScreen.tsx`

**الوصف:** التسجيل يحفظ `{name, nationalId, email, password}` كاملاً في `localStorage` تحت `diwan_registered_users_v1`. تسجيل الدخول يقرأ كلمة المرور النصية ويقارنها محلياً. لا يوجد hashing أو backend أو جلسة موثوقة.

**طريقة إعادة المشكلة:**
1. أنشئ حساب مستخدم من شاشة التسجيل.
2. افتح تخزين الموقع في المتصفح.
3. اقرأ المفتاح `diwan_registered_users_v1`.
4. تظهر كلمة المرور ورقم الهوية والبريد كنصوص قابلة للقراءة.

**الأثر:** كشف بيانات اعتماد وهوية المستخدم لأي JavaScript يعمل ضمن الأصل، أو لمستخدم الجهاز نفسه، ويجعل الحساب محلياً لذلك المتصفح فقط وليس حساباً حقيقياً آمناً.

**الإجراء المطلوب:** نقل إدارة المستخدمين إلى backend وقاعدة بيانات، تخزين password hash قوي فقط، وعدم وضع كلمات المرور أو بيانات الهوية الحساسة في localStorage.

## SAIF-003 — عالٍ — واجهة `/api/chat` تقبل `systemInstruction` من العميل بلا مصادقة

**المكان:** `api/chat.ts`

**الوصف:** endpoint الذكاء الاصطناعي لا يتحقق من جلسة مستخدم أو صلاحية، ويقبل `systemInstruction` من body ثم يدمجه مباشرة قبل رسالة المستخدم. أي عميل قادر على استدعاء endpoint يمكنه التحكم بتعليمات النظام واستهلاك مفاتيح Gemini الموجودة في الخادم.

**طريقة إعادة المشكلة:**
1. أرسل POST إلى `/api/chat` من عميل غير مسجل الدخول.
2. ضع `message` و`systemInstruction` من اختيارك.
3. الكود لا يحتوي على تحقق جلسة قبل استدعاء Gemini.

**الأثر:** استهلاك غير مصرح لحصة API، وإمكانية تجاوز شخصية/قيود الوكيل التي تتوقعها الواجهة، وزيادة خطر إساءة استخدام الخدمة.

**الإجراء المطلوب:** مصادقة الطلب خادمياً، rate limiting، عدم الثقة بـ`systemInstruction` القادم من المتصفح، وبناء تعليمات النظام المسموح بها على الخادم حسب الدور/المسار.

## ملاحظات الجولة

- تعذر إجراء اختبار نقر فعلي للنسخة الإنتاجية من بيئة الفحص الحالية لأن أداة التصفح لم تتمكن من فتح نطاق Vercel؛ لذلك لم أسجل أي عيب UI غير مثبت بالمشاهدة.
- فحص هذه الجولة ركز على الدخول، الجلسات، الخصوصية، وحدود API من المصدر البرمجي.
- الجولات التالية يجب أن توسع الفحص إلى المراجع، القضايا، رفع الملفات، الأزرار، الجوال، ومسارات المحاكم والتقارير، مع عدم تكرار العيوب أعلاه.
