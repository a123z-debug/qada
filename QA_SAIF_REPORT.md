# تقرير سيف لاختبار منصة أصول القضاء

**الحالة:** قيد الفحص المستمر  
**تاريخ بدء الجولة:** 2026-09-19  
**نسخة الإنتاج:** https://promptmaster-nine.vercel.app  
**المستودع:** a123z-debug/qada

## منهج الفحص
يتم فحص الواجهات، التنقل، الأزرار، النوافذ، الجوال، APIs، الذكاء الاصطناعي، الخصوصية، المراجع القانونية، وسلوك حالات الفشل. لا تعتبر الصفحة ناجحة لمجرد نجاح البناء.

## العيوب المؤكدة حتى الآن

### SAIF-001 — إظهار رقم الهوية كاملاً في العلامة المائية
- **الخطورة:** حرجة — خصوصية
- **الموضع:** `src/App.tsx` — SecurityWatermark
- **الوصف:** كانت العلامة المائية تعرض رقم الهوية الوطني كاملاً على الشاشة.
- **الحالة:** تم الإصلاح؛ أصبح المعروض آخر أربعة أرقام فقط.

### SAIF-002 — إرسال رقم الهوية إلى مزود الذكاء الاصطناعي في مراجعة القضاة
- **الخطورة:** حرجة — خصوصية
- **الموضع:** `api/judges-review.ts`
- **الوصف:** كان رقم الهوية يدخل في نص الطلب المرسل إلى نموذج الذكاء الاصطناعي رغم عدم الحاجة إليه لإجراء المراجعة القانونية.
- **الحالة:** تم الإصلاح؛ أزيل رقم الهوية من مخطط الطلب المرسل للنموذج.

### SAIF-003 — تقرير احتياطي يعطي انطباع اكتمال الفحص عند فشل الذكاء الاصطناعي
- **الخطورة:** عالية — موثوقية
- **الموضع:** `api/judges-review.ts`
- **الوصف:** كان fallback يعيد حالة «مكتمل ومستوفٍ للأصول» عند تعذر الفحص، ما قد يوحي بنتيجة قانونية لم تُفحص فعلياً.
- **الحالة:** تم الإصلاح؛ أصبحت الحالة «تعذر إكمال الفحص الآلي».

### SAIF-004 — قواعد قانونية جامدة وأمثلة غير موثقة داخل تعليمات النموذج
- **الخطورة:** عالية — سلامة المراجع
- **الموضع:** `server.ts`
- **الوصف:** وُجدت استنتاجات قانونية ثابتة وأسماء قضاة بشرية ضمن الـ system prompts، ما قد يدفع النموذج لتكرارها دون تحقق من المصدر والنسخة النافذة.
- **الحالة:** تم إصلاح الدفعة الأولى؛ استبدلت بقواعد تحقق مرجعي ومراجعين تحليليين محايدين. يستمر الفحص لبقية الملفات.

### SAIF-005 — نقص ضوابط الوصول في واجهة المستشار
- **الخطورة:** حرجة — تحكم وصول
- **الموضع:** `api/chat.ts`
- **الوصف:** كان مسار المستشار يقبل الطلب دون جلسة مستخدم، وكان يقبل `systemInstruction` من العميل.
- **طريقة إعادة المشكلة قبل الإصلاح:** إرسال POST مباشر إلى `/api/chat` دون جلسة، أو تمرير `systemInstruction` مخصص في جسم الطلب.
- **الإصلاح:** أصبح المسار يفرض جلسة خادمية موقعة، يرفض غير المسجل بـ401، يثبت تعليمات النظام على الخادم، ويتجاهل تعليمات النظام القادمة من العميل. أضيف أيضاً حد استخدام مؤقت لكل جلسة.
- **الحالة:** تم الإصلاح ودمجه في `master` عند commit `afd223d`؛ اجتاز كود التطبيق بناء Preview ناجحاً قبل نفاد حصة Vercel، والنشر الإنتاجي بانتظار توفر حصة النشر.


### SAIF-006 — بيانات دخول الإدارة داخل الواجهة والتحقق في المتصفح
- **الخطورة:** حرجة — مصادقة/خصوصية
- **الموضع:** `src/components/LoginScreen.tsx`
- **الوصف:** كانت بيانات اعتماد الإدارة مضمنة في JavaScript المنشور، وكان التحقق يتم بالكامل في المتصفح.
- **طريقة إعادة المشكلة قبل الإصلاح:** فتح مصدر الواجهة أو حزمة JavaScript والبحث عن ثابت بيانات الإدارة ثم تتبع شرط تسجيل الدخول.
- **الأثر:** أي شخص يستطيع قراءة الحزمة المنشورة كان يستطيع استعادة بيانات الاعتماد وتكوين جلسة إدارية منطقياً من جهة العميل.
- **الإصلاح:** أزيلت بيانات الإدارة من الواجهة. أصبح التحقق داخل `/api/auth` على الخادم باستخدام قيمة مشتقة أحادية الاتجاه، والجلسة تصدر من الخادم فقط داخل Cookie من نوع HttpOnly.
- **الحالة:** تم الإصلاح ودمجه في `master`؛ بيانات الدخول السابقة لم تعد جزءاً من آلية المصادقة الجديدة ولن تكون صالحة بعد نشر هذا الإصدار.

### SAIF-007 — حفظ كلمات مرور المستخدمين كنص صريح في localStorage
- **الخطورة:** حرجة — سرية بيانات الاعتماد
- **الموضع:** `src/components/LoginScreen.tsx`
- **الوصف:** كان سجل `diwan_registered_users_v1` يحفظ كلمة مرور كل مستخدم بصورتها الصريحة.
- **طريقة إعادة المشكلة قبل الإصلاح:** إنشاء مستخدم ثم فحص Local Storage في المتصفح.
- **الأثر:** أي سكربت يعمل في الصفحة أو أي شخص يصل إلى تخزين المتصفح يمكنه قراءة كلمات المرور.
- **الإصلاح:** أزيل حفظ كلمات المرور الصريحة. التحقق أصبح خادمياً مع PBKDF2، والمتصفح يحتفظ فقط بإثبات حساب مشفر وغير قابل للقراءة بدون مفتاح الخادم. كما تُحذف قيمة التخزين القديمة تلقائياً عند تحميل شاشة الدخول الجديدة.
- **الحالة:** تم الإصلاح ودمجه في `master`؛ النشر الإنتاجي بانتظار توفر حصة Vercel.

### SAIF-008 — الثقة في جلسة مستخدم مخزنة وقابلة للتعديل في localStorage
- **الخطورة:** عالية — تحكم وصول
- **الموضع:** `src/App.tsx`
- **الوصف:** كانت الواجهة تستعيد كائن الجلسة مباشرة من `diwan_user_session_v1` وتتعامل معه كجلسة موثوقة.
- **طريقة إعادة المشكلة قبل الإصلاح:** تعديل كائن الجلسة في Local Storage ثم إعادة تحميل الصفحة.
- **الأثر:** يمكن تزوير حالة الدخول أو الدور داخل الواجهة، ولا توجد هوية خادمية موثوقة مرتبطة بها.
- **الإصلاح:** أزيلت الثقة في Local Storage، وأصبحت استعادة الجلسة عبر `GET /api/auth` من Cookie موقعة وHttpOnly، والخروج يبطل الـCookie عبر الخادم.
- **الحالة:** تم الإصلاح في الكود الحالي للفرع، بانتظار التحقق والدمج.

### SAIF-009 — بقاء البيانات الحساسة القديمة في تاريخ مستودع عام
- **الخطورة:** حرجة — خصوصية
- **الموضع:** تاريخ Git للمستودع العام `a123z-debug/qada`
- **الوصف:** حذف البيانات من آخر نسخة لا يمحوها من الـcommits السابقة؛ يمكن الرجوع إلى النسخ التاريخية واستعادتها.
- **طريقة إعادة المشكلة:** فتح commit سابق يحتوي على شاشة الدخول قبل الإصلاح أو فحص تاريخ الملف.
- **الأثر:** تبقى المعرّفات/بيانات الاعتماد التاريخية مكشوفة حتى بعد إصلاح HEAD. تدوير بيانات الدخول يمنع استخدامها للمصادقة لكنه لا يمحو المعرّف الحساس من التاريخ.
- **الإصلاح الحالي:** تم تدوير بيانات اعتماد الإدارة وإيقاف الاعتماد على المعرّف الشخصي كبيان دخول، لذلك لن تصلح البيانات القديمة للدخول بعد نشر الإصلاح.
- **الإصلاح المتبقي:** إعادة كتابة تاريخ Git وتنظيف النسخ القديمة ثم تدوير أي أسرار مرتبطة بها. هذا إجراء تخريبي للتاريخ ويجب تنفيذه كعملية صيانة مستقلة.
- **الحالة:** الأثر التشغيلي لبيانات الدخول يعالج بهذا الإصلاح؛ أثر الخصوصية في تاريخ Git ما زال مفتوحاً.

## عناصر تحت الفحص
- جميع أزرار التنقل ومسارات الجوال.
- مركز المراجع والنسخ والبحث.
- رفع المستندات والمرفقات.
- المستشار وواجهات الذكاء الاصطناعي.
- مراجعة الأحكام/اللوائح والـ fallback.
- سلامة الروابط ومصادر المراجع.
- مفاتيح Gemini والتبديل الاحتياطي.
- أي بيانات تجريبية أو نتائج وهمية في واجهة الإنتاج.

## ملاحظة
هذا ملف عمل تراكمي. يضاف كل عيب مؤكد مرة واحدة مع طريقة إعادة المشكلة والحالة، ثم يصدر التقرير النهائي بعد انتهاء دورة الفحص.

## إصلاحات الجولة الحالية — 2026-09-19 مساءً

### SAIF-011 — إرسال رقم الهوية في المحادثة
- **الخطورة:** حرجة — خصوصية.
- **الحالة:** أعيد فتحه؛ الإصلاح السابق كان جزئياً.
- أزيل `clientNationalId` من `FloatingChatBot`، لكن مساحات الإداري والجزائي والعام ما زالت تُدخل رقم الهوية كاملاً داخل نص الرسالة نفسه قبل إرساله إلى `/api/chat`، كما أن مستودع الأحكام يبني مطالبة للمستشار تتضمن `سجل مدني: <رقم الهوية>`.
- **الأثر:** يصل رقم الهوية ضمن محتوى الطلب المرسل إلى مزود الذكاء الاصطناعي، رغم عدم ضرورته للتحليل القانوني.
- **الإصلاح المطلوب:** نزع الهوية من جميع prompts قبل الإرسال، واستبدالها بمعرّف محلي غير حساس أو حذفها كلياً من سياق النموذج.

### SAIF-012 — تجاهل مرفقات PDF/الصور في إنتاج Vercel
- **الحالة:** تم الإصلاح برمجياً.
- أصبح `api/chat.ts` يستقبل `messages[].attachments` ويحول الملفات إلى `inlineData` ويرسلها للنموذج مع النص.
- أضيفت تعليمات خادم تمنع الادعاء بقراءة مستند لم يُقرأ أو اعتماد نتيجة عند فشل التحليل.

### SAIF-016 — رد قانوني ثابت عند غياب استجابة الذكاء الاصطناعي
- **الحالة:** تم الإصلاح.
- أزيل الرد الذي يقرر مواعيد 60/30 يوماً، واستبدل برسالة فشل صريحة لا تتضمن حكماً أو ميعاداً قانونياً.

### SAIF-017 — منح درجة 80 عند فشل مراجعة الهيئة
- **الخطورة:** عالية — موثوقية.
- **الحالة:** أعيد فتحه؛ إصلاح الخادم صحيح لكن الواجهة تعيد إدخال الخطأ.
- `api/judges-review.ts` يعيد `scoreOutOf100: null` عند الفشل، لكن `normalizeJudgesReport` في `LegalReviewEditor.tsx` يستخدم `Number(score) || 80` ويضع 80 أيضاً عند غياب تفاصيل القاضي.
- **طريقة إعادة المشكلة:** مرّر تقريراً بدرجة `null` أو بدون درجة؛ الواجهة تطبّعها إلى 80.
- **الإصلاح المطلوب:** الحفاظ على `null` كـ «غير مقيم» ومنع أي درجة افتراضية عند الفشل أو نقص التقرير.

### SAIF-021 — تعميم مهلة 60 يوماً في حاسبة المادة (8)
- **الحالة:** تم الإصلاح مبدئياً.
- أضيف اختيار إلزامي لمسار الدعوى: إلغاء قرار إداري / حقوق وظيفية.
- لا تطبق الحاسبة مهلة الإلغاء على دعاوى الحقوق الوظيفية، ولا تصدر حكماً نهائياً بالسقوط أو القبول.
- النتائج موسومة بأنها حساب مبدئي يلزم مطابقته بالنص الرسمي النافذ.

### SAIF-022 — Prompt تحليل PDF منحاز لقضية وبدلات محددة
- **الحالة:** تم الإصلاح.
- أزيلت الافتراضات المسبقة الخاصة بالمادة (8) والمرسوم (م/37) وبدل الحاسب والمادة (16).
- التحليل يبدأ الآن بتحديد نوع المستند والاختصاص والموضوع، ثم يطلب السند النظامي المناسب فقط.


## حالة النشر للإصلاح الأمني
- **الكود:** مدمج في `master` عند `afd223d965e4825c9c995b9e5834c510ce726168`.
- **التحقق من البناء:** نجحت نسخة Preview لمشروع `qada` على نفس تغييرات كود التطبيق قبل آخر تحديثات التقرير.
- **الإنتاج `promptmaster-nine.vercel.app`:** لم يستقبل الإصلاح بعد لأن Vercel رفض deployment بعد تجاوز حد الخطة المجانية لأكثر من 100 عملية نشر في اليوم. هذا فشل حصة نشر وليس Build Error.
- **SAIF-009:** يظل تنظيف تاريخ Git القديم مطلوباً لإزالة الأثر التاريخي للبيانات الحساسة؛ تدوير المصادقة يمنع استخدام بيانات الدخول القديمة لكنه لا يمحوها من commits السابقة.


## جولة تقوية المصادقة v3
### SAIF-023 — بيانات الجلسة كانت موقعة دون تشفير
- **الخطورة:** عالية — خصوصية.
- **الوصف:** الجلسة السابقة منعت التعديل بالتوقيع لكنها أبقت الحمولة قابلة للقراءة من قيمة الـCookie.
- **الإصلاح:** ترقية الجلسة إلى `qada_session_v3` وتشفير كامل الحمولة بـ AES-256-GCM مع مفتاح مشتق خادمياً، وإبطال Cookie الإصدار السابق عند الدخول والخروج.
- **الحالة:** تم الإصلاح ودمجه في `master` ضمن commit `dc75b8d562078438d05b79215e77e054d41a432d`.

### SAIF-024 — بيانات الإدارة/الدعوة التي تم تداولها تحتاج تدويراً
- **الخطورة:** حرجة — بيانات اعتماد.
- **الإصلاح:** تدوير قيم الإدارة والدعوة إلى قيم جديدة عالية العشوائية؛ لا تحفظ القيم الخام في Git، وإنما تحفظ البصمات فقط.
- **الحالة:** تم الإصلاح على فرع التقوية v3؛ القيم الجديدة تُسلّم لصاحب المنصة خارج المستودع.

### SAIF-025 — عدم وجود تقييد لمحاولات المصادقة
- **الخطورة:** عالية — حماية الدخول.
- **الإصلاح:** إضافة تقييد لمحاولات التسجيل والدخول والإدارة، مع `429` و`Retry-After` بعد تجاوز الحد، في Vercel والخادم المحلي.
- **ملاحظة:** الحاجز الحالي داخل ذاكرة كل instance؛ لحماية موزعة كاملة عند التوسع يلزم مخزن rate-limit مركزي أو Vercel Firewall.
- **الحالة:** تم الإصلاح مبدئياً.


## حالة تقوية v3 بعد الدمج
- **Commit الأمني:** `dc75b8d562078438d05b79215e77e054d41a432d`.
- **الحالة البرمجية:** مدمج في `master`.
- **الجلسات:** `qada_session_v3` مشفرة بـ AES-256-GCM، وCookie الإصدار السابق تُبطل عند الدخول والخروج.
- **بيانات الإدارة والدعوة:** دُوّرت، والقيم الخام غير موجودة في المستودع.
- **النشر:** فحص Vercel الحالي يفشل قبل البناء بسبب `api-deployments-free-per-day` بعد تجاوز أكثر من 100 عملية نشر؛ لذلك لا تعتبر نسخة الإنتاج محدثة حتى ينجح Deployment جديد.
- **تاريخ Git القديم:** ما زال SAIF-009 مفتوحاً؛ تدوير بيانات الاعتماد يعطل استخدامها، لكنه لا يزيل القيم التاريخية من commits القديمة.


## جولة الحصر الشامل قبل دفعة الإصلاح — 2026-09-19

> هذه الجولة **حصر فقط**. لم تُنفذ إصلاحات البنود التالية بعد، باستثناء تحديث هذا التقرير. الأولوية غداً: الحرجة ثم العالية ثم المتوسطة، مع اختبار الإنتاج بعد كل دفعة.

### SAIF-026 — مسودات المحاكم مشتركة بين المستخدمين على نفس المتصفح
- **الخطورة:** حرجة — خصوصية وعزل مستخدمين.
- **المواضع:** `AdministrativeWorkspace.tsx`, `CriminalWorkspace.tsx`, `GeneralWorkspace.tsx`.
- **الوصف:** كل مساحة تستخدم مفتاح Local Storage ثابتاً مثل `diwan_administrative_draft_v2` دون تضمين `session.id`. الخروج لا يمسح هذه المفاتيح.
- **طريقة إعادة المشكلة:** سجّل بحساب A، أدخل قضية وهوية ووقائع، اخرج، ثم سجّل بحساب B على نفس الجهاز وافتح نفس المساحة؛ تُستعاد مسودة A.
- **الإصلاح المطلوب:** مفاتيح scoped لكل مستخدم، تنظيف/ترحيل التخزين القديم، وعدم حفظ بيانات قضائية حساسة في Local Storage بلا تشفير.

### SAIF-027 — ملفات القضايا والمرفقات الحساسة محفوظة كنص صريح في Local Storage
- **الخطورة:** حرجة — خصوصية/فقد بيانات.
- **المواضع:** `App.tsx` ومفاتيح القضايا والمسودات و`diwan_pending_attachments_v1`.
- **الوصف:** بيانات القضية تشمل الهوية والوقائع والأحكام والدفوع، كما تحفظ المرفقات Base64 في Local Storage. في المقابل تعرض الواجهة عبارات «مساحة عمل مشفرة» و«عزل الملفات مفعل».
- **الأثر:** البيانات قابلة للقراءة من أي JavaScript يعمل في الأصل نفسه، وتضيع عند مسح بيانات المتصفح، ولا يوجد تخزين خادمي فعلي للقضايا.
- **الإصلاح المطلوب:** تخزين خادمي مشفر ومقيد بالمستخدم، أو على الأقل تشفير محلي واضح مع تقليل الادعاءات الأمنية حتى اكتمال البنية.

### SAIF-028 — لوحة الإدارة «مرفوعات جميع المستخدمين» لا تعرض جميع المستخدمين
- **الخطورة:** عالية — وظيفة أساسية مضللة.
- **المواضع:** `WelcomeScreen.tsx` → `onOpenAdminOverview`, و`App.tsx`/ `JudgmentRepositoryModal.tsx`.
- **الوصف:** زر الإدارة يفتح نفس `JudgmentRepositoryModal` ويمرر فقط `judgmentRecords` المحملة من Local Storage الخاص بجلسة المدير الحالية. لا توجد قاعدة مستخدمين/قضايا خادمية يمكن جمعها.
- **طريقة إعادة المشكلة:** أنشئ قضية بحساب مستخدم، اخرج وادخل كمدير على نفس/جهاز آخر؛ لا يوجد مسار يجلب جميع قضايا المستخدمين من الخادم.
- **الإصلاح المطلوب:** مخزن مركزي مع صلاحيات Admin وAPI محمي للاستعلام، أو إزالة الزر/إعادة تسميته حتى تتوفر الوظيفة.

### SAIF-029 — أداة «حوّل قصتي إلى تكييف نظامي» تستدعي API غير موجود في Vercel
- **الخطورة:** عالية — وظيفة مكسورة.
- **الموضع:** `PlainStoryInput.tsx` يستدعي `POST /api/convert-story`.
- **الدليل:** مجلد `api/` في نشر Vercel يحتوي `auth.ts`, `chat.ts`, `judges-review.ts`, `_auth.ts` فقط؛ `convert-story` موجود كمسار Express داخل `server.ts` وليس كـ Vercel Function.
- **الأثر:** المسار الأساسي للمستخدم الذي يكتب قصته بشكل عادي يفشل في الإنتاج.
- **الإصلاح المطلوب:** إنشاء `api/convert-story.ts` بنفس ضوابط المصادقة/المراجع أو نقل الوظيفة إلى `/api/chat` بعقد واضح.

### SAIF-030 — رفع الملفات داخل مساحات المحاكم لا يقرأ PDF/DOC/DOCX/الصور فعلياً
- **الخطورة:** عالية — وظيفة المرفقات.
- **المواضع:** مساحات الإداري والجزائي والعام.
- **الوصف:** حقول الرفع تقبل `.pdf,.doc,.docx,.txt,image/*` ثم تستخدم `FileReader.readAsText(file)` وتأخذ أول 1200 حرف. الملفات الثنائية تتحول إلى نص غير صالح ولا تُرسل كمرفق بصري/وثيقة للنموذج.
- **الإصلاح المطلوب:** توحيد الرفع عبر مكوّن المرفقات الحقيقي وإرسال bytes/MIME المدعومة أو تحويل DOC/DOCX على الخادم قبل التحليل.

### SAIF-031 — حد 25MB في الواجهة غير قابل للعمل عبر Vercel Function الحالية
- **الخطورة:** عالية — رفع الملفات.
- **الموضع:** `PdfUploadModal.tsx` يسمح حتى 25MB ويرسل Base64 داخل JSON إلى `/api/chat`.
- **الدليل الخارجي:** Vercel يحدد حمولة الطلب إلى Function بـ 4.5MB؛ Base64 يزيد الحجم أكثر. مرجع: https://vercel.com/docs/errors/function_payload_too_large
- **طريقة إعادة المشكلة:** اختر PDF بعدة ميجابايت ضمن «25MB المسموح» واضغط التحليل؛ الطلب قد يُرفض 413 قبل وصوله إلى الدالة.
- **الإصلاح المطلوب:** رفع مباشر إلى Blob/Object Storage أو Gemini Files API ثم تمرير مرجع الملف، وضبط حد الواجهة على الواقع.

### SAIF-032 — الواجهة تقبل DOC/DOCX بينما مسار Gemini الحالي لا يدعمهما كـ inlineData موثوق
- **الخطورة:** متوسطة/عالية — توافق ملفات.
- **الموضع:** `PdfUploadModal.tsx` يقبل `.doc,.docx` و`api/chat.ts` يمرر MIME كما هو.
- **الدليل الخارجي:** قائمة تطبيقات Gemini المدعومة للمدخلات المباشرة تتضمن `application/pdf` و`application/json` ولا تسرد DOC/DOCX؛ الأنواع غير المدعومة تعيد خطأ. مرجع: https://ai.google.dev/gemini-api/docs/file-input-methods
- **الإصلاح المطلوب:** تحويل DOC/DOCX إلى PDF/نص أو رفضها برسالة صريحة.

### SAIF-033 — زر «إرفاق بالمحادثة فقط» لا يرفق الملف بالمحادثة
- **الخطورة:** عالية — زر غير مربوط بالوظيفة المعلنة.
- **المواضع:** `PdfUploadModal.tsx` + `App.tsx`.
- **الوصف:** الزر يستدعي `onAddAttachments`، لكن `App` يحفظ المرفقات في `diwan_pending_attachments_v1` ويفتح «قضيتي» فقط؛ لا يمررها إلى `FloatingChatBot.externalAttachments`.
- **الإصلاح المطلوب:** تمرير المرفقات إلى حالة المستشار وفتح المحادثة، أو تغيير اسم الزر إذا كان الهدف ملف القضية.

### SAIF-034 — بيانات وقضايا افتراضية جاهزة تُحقن في كل مستخدم جديد
- **الخطورة:** عالية — بيانات وهمية/تحيز قانوني.
- **المواضع:** `AdministrativeWorkspace.tsx`, `CriminalWorkspace.tsx`, `GeneralWorkspace.tsx`.
- **أمثلة مؤكدة:** وزارة الدفاع وبدل مكافحة الإرهاب والمرسوم م/37 وتاريخ تظلم ثابت؛ افتراض بطلان قبض وتفتيش ومواد محددة؛ مطالبة عقد مقاولة بقيمة 150,000 ريال.
- **الأثر:** المستخدم الجديد يبدأ بقضية مختلقة ويمكن أن يولّد مذكرة مبنية عليها قبل استبدال كل الحقول.
- **الإصلاح المطلوب:** حقول فارغة/أمثلة Placeholder غير قابلة للإرسال، وتوليد الأسانيد بعد إدخال الوقائع لا قبلها.

### SAIF-035 — عند فشل الذكاء الاصطناعي تُنشأ مذكرات قانونية ثابتة وكأنها نتيجة ناجحة
- **الخطورة:** حرجة — سلامة قانونية/حالة خطأ.
- **المواضع:** مساحات الإداري والجزائي والعام.
- **الوصف:** عند فشل `/api/chat` أو خروج رد فارغ، الكود يبني `fallback` أو `fallbackText` يتضمن وقائع ومواد وطلبات قانونية ثم يفتح وضع المراجعة، بدلاً من إظهار فشل التحليل.
- **أمثلة:** م/37 وقرار الشورى 63/92؛ المادة 35 وبطلان القبض؛ المواد 128/129/138 ومبلغ عقد.
- **الإصلاح المطلوب:** لا يوجد fallback قانوني موضوعي. اعرض خطأ واضحاً واحتفظ بمدخلات المستخدم دون إضافة مواد أو وقائع غير متحققة.

### SAIF-036 — مؤشر «سلامة البناء القضائي 78%» بيانات وهمية ثابتة
- **الخطورة:** متوسطة — تضليل واجهة.
- **الموضع:** `WelcomeScreen.tsx`.
- **الوصف:** تعرض الصفحة الرئيسية داخل مساحة المستخدم نسبة 78% ثابتة لا ترتبط بأي قضية أو فحص.
- **الإصلاح المطلوب:** حذف المؤشر أو ربطه بنتيجة فحص حقيقية ذات تعريف واضح.

### SAIF-037 — الحارس الأمامي يعطل النسخ والقص وتحديد الكل والحفظ والطباعة من لوحة المفاتيح
- **الخطورة:** متوسطة/عالية — استخدام وإتاحة.
- **الموضع:** `src/main.tsx` → `FrontendGuard`.
- **الوصف:** يمنع أحداث `copy`, `cut`, contextmenu ويمنع Ctrl/Cmd + C/X/A/S/P بالإضافة لأدوات المطور.
- **الأثر:** محرر قانوني يفقد اختصارات أساسية، ولا يوفر هذا حماية حقيقية للمصدر أو البيانات.
- **الإصلاح المطلوب:** إزالة منع الاختصارات والإبقاء على ضوابط أمنية خادمية حقيقية فقط.

### SAIF-038 — محلل SSE في أربع واجهات لا يحتفظ بالأحداث المجزأة بين حزم الشبكة
- **الخطورة:** عالية — استقرار الردود.
- **المواضع:** `FloatingChatBot` ومساحات الإداري والجزائي والعام.
- **الوصف:** كل `reader.read()` يُحوّل مباشرة إلى `chunk.split('\n')`. إذا انقسم سطر `data: {...}` بين حزمتين، يُفشل JSON ويُلحق جزءاً خاماً أو يفقد الجزء الثاني.
- **الأثر:** الرد الطويل قد يظهر مشوهاً/مبتوراً رغم نجاح API.
- **الإصلاح المطلوب:** buffer تراكمي حتى فاصل SSE الكامل أو استخدام parser قياسي/استجابة JSON إذا لم يكن هناك Streaming حقيقي.

### SAIF-039 — زر حاسبة المادة 8 «إدراج في الاستشارة القضائية» موصول بـ no-op
- **الخطورة:** متوسطة/عالية — زر غير مربوط.
- **الموضع:** `App.tsx`.
- **الوصف:** `Article8CalculatorModal` يمرر النص إلى `onInsertToPrompt(generatedText)` لكن `App` يمرر `() => setIsArticle8Open(false)` ويتجاهل النص.
- **الإصلاح المطلوب:** تمرير التقرير إلى `requestAssistant` أو إلى الحقل المستهدف.

### SAIF-040 — البحث في مركز المراجع يمكن أن يعرض مرجعاً لا يطابق نتائج البحث
- **الخطورة:** متوسطة — بحث/تنقل.
- **الموضع:** `LegalReferencesModal.tsx`.
- **الوصف:** `selected` يفضّل دائماً `selectedId` من القائمة الكاملة قبل `systems[0]`. عند تغيير البحث/الفئة لا يُعاد ضبط `selectedId`؛ قد تقول القائمة «لا توجد نتائج» بينما تبقى تفاصيل مرجع قديم معروضة.
- **الإصلاح المطلوب:** إن لم يكن العنصر المحدد ضمن النتائج المفلترة، اختر أول نتيجة أو اعرض حالة فارغة.

### SAIF-041 — شارة «مطابق بمصدر رسمي» تُطبّق على حزمة نصوص داخلية غير متحققة كلها
- **الخطورة:** عالية — سلامة المراجع.
- **المواضع:** `legalReferences.ts`, `legalRetrieval.ts`, `LegalReferencesModal.tsx`.
- **الوصف:** `verificationStatus: official` يُستخدم إذا كان للسجل رابط رسمي، ثم تُعرض الشارة «مطابق بمصدر رسمي» ويُمرر `lawText/executiveText/amendmentsText` الداخلي إلى النموذج تحت عنوان «المراجع الرسمية المسترجعة». وجود رابط للنظام لا يثبت أن كل النصوص والتعديلات/الملاحظات الداخلية مطابقة حرفياً لذلك المصدر.
- **الإصلاح المطلوب:** حالة تحقق على مستوى كل مقطع/مادة وإصدار، وتمييز «رابط رسمي للنظام» عن «نص تم مطابقته حرفياً».

### SAIF-042 — الواجهة تنسب مراجعة الذكاء الاصطناعي إلى «قضاة» و«اعتماد قضائي»
- **الخطورة:** عالية — ثقة المستخدم/وصف الوظيفة.
- **المواضع:** `LegalReviewEditor.tsx`, `JudgesCassationReviewPanel.tsx`, `CassationJudgesPanel.tsx`.
- **أمثلة:** «فضيلة القاضي»، «هيئة قضاة النقض والاستئناف والمحكمة العليا»، «النص معتمد قضائياً»، «جاهز للإيداع المباشر في معين/ناجز».
- **الأثر:** قد يفهم المستخدم أن المخرج راجعه قضاة حقيقيون أو حصل على اعتماد قضائي.
- **الإصلاح المطلوب:** تسميات صريحة مثل «وكلاء مراجعة آلية/محاكاة مراجعة» ومنع وصف أي مخرج بأنه معتمد أو جاهز للإيداع تلقائياً.

### SAIF-043 — منطق «سلامة المستند» يتجاهل أخطاء المرفقات وقد يعلن الجاهزية رغم وجودها
- **الخطورة:** عالية — موثوقية التقرير.
- **الموضع:** `JudgesCassationReviewPanel.tsx`.
- **الوصف:** `isSoundDocument` يعتبر المستند سليماً إذا كانت `cassationErrors` و`claimErrors` فارغتين أو إذا كانت `overallStatus === 'جاهز للإيداع'`، دون اشتراط خلو `attachmentErrors` أو `missingRequiredDocs`.
- **الإصلاح المطلوب:** شرط سلامة محافظ يشمل جميع مجموعات الأخطاء والمرفقات وعدم وجود أي فشل أو تقييم ناقص.

### SAIF-044 — فشل «هيئة المراجعة» يُسجل في console فقط بلا رسالة للمستخدم
- **الخطورة:** متوسطة — حالات الخطأ.
- **الموضع:** `LegalReviewEditor.tsx`.
- **الوصف:** `handleRunJudgesAudit` يمسك الخطأ ويستدعي `console.error` ثم يوقف التحميل؛ لا تظهر حالة فشل أو سبب أو زر إعادة محاولة واضح.
- **الإصلاح المطلوب:** حالة `auditError` مرئية ومنع بقاء تقرير قديم وكأنه أحدث نتيجة.

### SAIF-045 — مفتاح تشفير الحسابات/الجلسات يعتمد احتياطياً على GEMINI_API_KEY
- **الخطورة:** عالية — استمرارية ومبدأ فصل الأسرار.
- **الموضع:** `api/_auth.ts` → `getRootSecret`.
- **الوصف:** إذا لم يوجد `AUTH_SECRET`، يُشتق مفتاح المصادقة والتشفير من `GEMINI_API_KEY`. تدوير مفتاح Gemini عند تسريب/انتهاء الحصة يبطل الجلسات ويجعل account proofs القديمة غير قابلة للفك.
- **الإصلاح المطلوب:** جعل `AUTH_SECRET` إلزامياً مستقلاً وعدم تشغيل المصادقة دون وجوده.

### SAIF-046 — الحسابات نفسها محلية للمتصفح وليست سجلاً خادمياً
- **الخطورة:** عالية — حسابات/استرجاع/إدارة.
- **الموضع:** `LoginScreen.tsx` + `api/_auth.ts`.
- **الوصف:** الخادم لا يحتفظ بسجل حساب؛ المتصفح يحتفظ بـ `accountProof`. الدخول من جهاز جديد يفشل لعدم وجود proof، ولا يوجد استرجاع حساب أو تعطيل مستخدم مركزي أو منع تسجيل نفس البريد على أجهزة مختلفة.
- **الإصلاح المطلوب:** قاعدة مستخدمين خادمية (hashed passwords/identity provider)، إدارة دعوات وصلاحيات، واسترجاع/تعطيل مركزي.

## عناصر يلزم تحققها تفاعلياً بعد عودة نشر Vercel
- الجوال: فتح/إغلاق القوائم والنوافذ، لوحة المفاتيح، التمرير، Safe Areas على iPhone.
- كل الروابط الخارجية للمصادر الرسمية وحالات 404/redirect.
- تسجيل الدخول/الخروج الفعلي بعد نشر `qada_session_v3`.
- رفع PDF حقيقي صغير وكبير، صورة، DOCX، وسلوك 413.
- مسار `/api/convert-story` بعد إنشائه.
- إعادة اختبار SSE تحت شبكة بطيئة/مجزأة.
- اختبار جميع الخدمات الاثنتي عشرة من Sidebar end-to-end وعدم الاكتفاء بظهور الصفحة.
- اختبار الأخطاء: 401/403/413/429/500/502 وانقطاع Gemini ونفاد المفاتيح.
