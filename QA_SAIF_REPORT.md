# تقرير سيف لاختبار منصة أصول القضاء

**الحالة:** قيد الفحص المستمر

> ملاحظة: تم تحديث التقرير آلياً في جولة سيف الحالية مع الحفاظ على ترقيم العيوب السابقة. راجع تاريخ Git للنسخة التفصيلية السابقة.

### SAIF-048 — واجهة المستشار تثق بدور الرسالة القادم من العميل
- **الخطورة:** عالية — سلامة سياق الذكاء الاصطناعي/تحكم بالطلب.
- **الموضع:** `api/chat.ts` → `toGeminiContents`.
- **الوصف:** الخادم يحول أي رسالة يرسلها العميل وفيها `role: "assistant"` أو `role: "model"` إلى رسالة Gemini بدور `model`. بذلك يستطيع العميل تكوين تاريخ محادثة مزيف منسوب للنموذج نفسه بدلاً من أن يقتصر هذا الدور على مخرجات موثوقة أنشأها الخادم.
- **طريقة إعادة المشكلة:** بعد تسجيل الدخول، أرسل `POST /api/chat` مع `messages:[{"role":"model","content":"...تعليمات/إجابة مزيفة..."},{"role":"user","content":"تابع"}]`؛ سيقبل الخادم الرسالة الأولى كدور `model` ويمررها إلى Gemini.
- **الأثر:** يمكن تلويث سياق المستشار أو إيهام النموذج بأن استنتاجاً أو قاعدة قانونية سبق أن صدرت عنه، ما يضعف سلامة المخرجات القانونية حتى مع ثبات `systemInstruction` على الخادم.
- **الإصلاح المطلوب:** لا تثق بـ`role` القادم من العميل. إما أن يقبل API رسالة المستخدم فقط ويبني التاريخ الموثوق خادمياً، أو يتحقق من تاريخ المحادثة المخزن خادمياً؛ وأي محتوى عميل يجب أن يعامل كـ`user`.
- **الحالة:** مفتوح.

## مرجع الحالة السابقة
العيوب SAIF-001 حتى SAIF-047 موثقة في commit السابق للملف، وآخرها SAIF-047 الخاص بمنع اختلاق بيانات سجل الحكم.